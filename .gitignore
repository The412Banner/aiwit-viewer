*.iml
.gradle/
local.properties
.idea/
.DS_Store
build/
captures/
.externalNativeBuild
.cxx
*.apk
*.aab
*.keystore

# Native libraries bundled in jniLibs/ ARE committed — they're required at
# runtime and the build needs them. See LIBS_NOTICE.md for provenance.
